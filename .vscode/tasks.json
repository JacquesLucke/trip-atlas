{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "cargo build",
      "problemMatcher": ["$rustc"],
      "label": "Build Server (Debug)",
      "options": {
        "cwd": "${workspaceFolder}/server"
      }
    }
  ]
}
